# Claude Code 이관 작업 목록 — 대한민국플라잉디스크연맹 홈페이지

> 작성 2026-08-31 · 저장소 `dododoyyya-dot/portal` (GitHub Pages)
> **이 목록의 작업은 Claude Code에서, 나머지는 지금 쓰시던 대화창에서** 진행하시면 됩니다.
> 각 항목의 파일·줄번호·근거는 실제 라이브 파일을 내려받아 확인한 값입니다.

---

## 0. 시작 전 — 저장소 루트에 `CLAUDE.md` 먼저 넣기

같이 드린 `CLAUDE.md`를 저장소 최상단에 두고 커밋하십시오.
여기에 **기존 기능 무손상 원칙 · 검증 절차 · 손대면 안 되는 것 · 데이터 구조**가 들어 있습니다.
이게 없으면 새 환경에서 같은 실수를 처음부터 다시 겪습니다.

---

## 1순위 — 지금 방식으로는 사실상 불가능한 것 (파일 다수 동시 수정)

수동 업로드는 파일 하나당 왕복이라, 아래 세 건은 지금까지 계속 밀렸습니다.

### 1-1. 모바일 표 가로스크롤 미적용 🔴

`style.css`에 `.tblwrap` 규칙은 이미 있는데 **표를 감싸는 마크업이 없어서 적용이 안 됩니다.**

| 파일 | `<table>` 개수 | `.tblwrap` 적용 |
|---|---|---|
| **admin.html** | 24 | **0** |
| mypage.html | 4 | 0 |
| region.html | 4 | 0 |
| staff.html | 3 | 0 |
| competition.html | 10 | 4 (일부만) |

**작업**: 각 `<table class="tbl">`을 `<div class="tblwrap">…</div>`로 감싸기.
**주의**: admin.html은 495KB에 표 24개 — 표별로 렌더 위치가 달라 일괄 치환 전에 각 표가 이미 스크롤 컨테이너 안에 있는지 확인할 것.
**검증**: 390px 뷰포트에서 `document.scrollWidth <= innerWidth` 확인 (기존 측정 스크립트 방식 재사용).

### 1-2. `login_upload.html` 캐시 버전 문자열 미갱신 🔴

```
28개 페이지 : style.css?v=20260827m   ← 갱신됨
login_upload.html : style.css?v=20260726e   ← 혼자 옛날 버전
```

→ **이 페이지에는 CSS 수정이 영원히 도달하지 않습니다.** 한 줄 수정.

### 1-3. `kfdf.js` 버전 문자열 불일치

```
19개 페이지 : kfdf.js?v=20260808
 5개 페이지 : kfdf.js?v=20260821
```

공용 JS를 고쳐도 19개 페이지는 옛 캐시를 씁니다. 전 페이지 통일 필요.

---

## 2순위 — 남은 6대 기능 (2건)

### 2-1. STEP 4 · 2단계 중앙 결재 (가장 큰 작업)

**범위 (회장님 확정)**: 시도임원의 ①회원 가입승인·거절 ②회원 정보수정 ③자격증 최종 보고·승인
→ 중앙 승인 전까지 보류. **클럽·임원·대회 운영은 기존대로 즉시 반영** (범위 확대 금지).

**재사용할 선례**: `license.html`에 **이미 작동 중인 2단계 승인 패턴**이 있습니다.
- 비중앙이 공고 등록 → `licenseNotices`에 `approval:'대기'`, `status:'중앙승인 대기'` → 회원에게 비공개
- 결과보고 제출도 동일 (license.html:541-546) — 중앙 승인 전 합격자 발급 불가

→ 이 `approval` / `status` 필드명 패턴을 **그대로** 회원 가입승인·정보수정으로 확장.
새 구조: `users.pendingChanges` + admin.html에 **승인 센터 탭**.

**같이 나올 산출물**: Firestore 규칙 문안 (적용은 회장님이 콘솔에서 직접).

⚠ **교육 안내서와 충돌**: 현재 안내서 4·5·6장이 "승인 즉시 반영"으로 쓰여 있습니다. 도입 시 개정 필요.

### 2-2. STEP 6 · 회원 고유번호 900번부터

**범위 (회장님 확정)**: 승인 회원만, 가입순으로 900부터.
**패턴**: 기존 `ageMig` / `qualFix` 도구와 동일하게 — 미리보기 → 백업(JSON 다운로드) → 1명 시험 → 선택 적용 → 되돌리기.
**참고**: `counters` + `nextSeq()` 가 이미 있음. `users.memberNo` 신규 필드.

---

## 3순위 — 실측으로 확인된 잠복 버그 3건

### 3-1. 확정 명단이 계정(uid) 단위 🔴 **실측 확인**

`competition.html:513` — `(s.confirmed||[]).indexOf(c.uid)`

신청은 **건 단위(pid)** 인데 확정은 **계정 단위(uid)** 입니다.
→ **보호자가 자녀 둘을 신청하면, 한 명만 확정해도 둘 다 "🏅 확정"으로 표시됩니다.**

손볼 곳: `competition.html` 513 · 626 · 1942 · 2653
기존 `confirmed[]` 데이터를 깨지 않으려면 pid 배열을 새로 두고 uid 폴백을 남기는 방식 권장.

### 3-2. `paperConsents` 키가 명단 순번(index) 기반 ⚠

현재 키: `{팀pid}_{명단 인덱스}` (예: `team_demo_0`)
→ **명단에서 한 명을 빼면 그 뒤 사람들의 동의 기록이 한 칸씩 밀립니다.**

2026-08-31에 "명단이 바뀌면 저장 먼저 하세요" 경고로 **당장의 사고는 막아 두었지만**, 근본 해결은
이름+생년월일 기반 안정 키로 마이그레이션하는 것입니다. (기존 키 폴백 유지 필수)

### 3-3. `claimed[]` 배열 read-modify-write 동시성 ⚠

참가 신청이 공고 문서 안의 **배열**이라, 상태 하나 바꾸려면 배열 전체를 읽고 다시 씁니다.
→ **담당자 두 명이 동시에 처리하면 한쪽 변경이 사라집니다.**

`reviewStatus` · `paperConsents` 는 이미 맵 필드라 안전합니다. 같은 방식으로 전환.
`claimed[]` 자체는 남기고 **변경이 잦은 필드만** 맵으로 빼는 쪽이 안전합니다 (payStatus 등).

---

## 4순위 — 정리·조사

### 4-1. `migrate_staff.html` 공개 URL 노출
일회성 데이터 이전 도구가 공개 URL에 남아 있고 `compSlots` → `competitionStaff` 대량 쓰기를 합니다.
`isAdmin()` 검사가 **클라이언트에만** 있어 규칙이 약하면 우회 가능.
→ 제거 또는 관리자 전용 경로로 이동. **회장님 승인 후 진행.**

### 4-2. "대시보드에 자격증 공고만 보임" 원 신고 재조사
admin.html 대시보드 코드에 자격증 관련 코드가 전혀 없어 재현 실패했습니다.
재발 시 해당 시도임원 계정으로 **어느 탭이 비고 어느 탭이 채워지는지** 확인 필요.

---

## Claude Code로 옮기지 마실 것 (지금 대화창이 더 낫습니다)

| 항목 | 이유 |
|---|---|
| 휴대폰 스크린샷 진단 | 사진 한 장 던지고 "이거 왜 이래"가 제일 빠릅니다 |
| 회장님 지시사항 해석·전달 | 카톡 화면 그대로 전달 가능 |
| 임원 대상 사용법 안내문 | 프로젝트 문서 17건이 여기 쌓여 있습니다 |
| 연습 사이트(sandbox) 갱신 | zip 전달이 편합니다 |
| Firebase 콘솔 규칙 문안 | 규칙 적용은 어차피 회장님 직접 |

---

## 보류 — 회장님 직접 (어느 쪽에서도 제가 못 함)

- **[최우선] Firestore / Storage 보안 규칙 실측**
  `privateInfo`에 주민등록번호, Storage에 자격증·재직증빙·**미성년 동의서 스캔본**이 있습니다.
  로그인한 일반 회원이 *타인의* `privateInfo`를 읽을 수 있는지 Rules Playground로 확인.
- Google Cloud Console — API 키 HTTP 리퍼러 제한
- EmailJS 도메인 allowlist

---

## 2026-09-07 — 부산연맹 문현민 회장 통화 3건 지시서(A~I) 진행 현황

| 코드 | 상태 | 커밋 | 비고 |
|---|---|---|---|
| A 타인 명단·수당 비노출 | ✅ 완료 | 9b8f9d3 | 관리자 화면 DOM 완전 동일(A/B 4계정). 2단계(규칙·집계 비정규화)는 `제안_A_심판공고_신청서열람범위_v1.txt` |
| B 날짜별 종목·시간·장소 | ✅ 완료 | 7269547 | `dates[i].title/location/time/gatherTime`, 비면 공고 값 폴백. 수정 시 `completed` 보존 버그도 함께 수정 |
| C 모바일 상세 첫 화면 | ✅ 완료 | 3f1e756 | 원인: `?slot=`/`?post=` 단독 상세 페이지 위에 일정 목록·안내 블록이 그대로 남음 → 760px 이하에서만 숨김. PC 무변경 |
| D-1 학교스포츠클럽 엑셀 업로드 | ✅ 완료 | 9996ec6 | admin 탭 8. ExcelJS 사용(admin에는 SheetJS 없음). 미가입 → `schoolClubResults` 보류. 경력점수 보류(`noPoints`) |
| D-3 선수 경력·내 기록 찾기·증명서 3종 | ✅ 완료 | 934bdce | 직인 찍음 + 외부 주최 확인 문구(admin 설정으로 끌 수 있음) |
| D-2 대회 결과 페이지 | ✅ 완료 | eb2c72c | `results.html`, 이름 기본 홍*동(admin 설정), `schoolClubEvents` 공개 요약 |
| E 심판 경력 엑셀 | ✅ 완료 | a65c85c | 같은 도구의 '심판·운영 경력' 모드. 회원번호→이메일→성명+생년월일, 중복 건너뜀 |
| G-1 관할 키 자동 | ✅ 완료 | 0248058 | 가입 시는 이미 저장 중이었음(login.html). 승인 시 비어 있으면 채움. 공용 파일 이동 안 함 |
| D-4 연동 대회 공고 | ✅ 완료 | 0cdd4e8 | 대회 성격 `compKind`, 학교팀 자동완성+`teamSchoolCode`, 신청 현황 명단 대조 배지 |
| G-2 서명 안내 · G-3 열람 축소 | ⏸ 제외 | — | 회장님 결정(2026-09-07): 시도임원 주민번호·계좌 열람 현상 유지 |
| H 모바일 최적화 | ✅ 점검 | — | 공개 22페이지 390px `scrollWidth==innerWidth` 전부 통과(로그아웃 상태). 로그인 화면(admin·mypage 표)은 실계정 확인 필요. C의 상세 레이아웃 개선 포함 |
| F 아이디 가입 | 📄 보고서 | — | `보고_F_초중학생_아이디가입_설계안_v1.txt` (코드 변경 없음) |
| I 후원 배너 | 📝 메모 | — | index.html 홈 배너 틀(ed73cbe) 재사용 — "후원사 배너 슬롯(관리자 등록·기간·링크)" 작업으로 이어질 수 있음. 손대지 않음 |

**회장님이 할 일**: ① 확인 후 push 승인 ② `제안_D_규칙_v19.txt` 게시(보류함·결과 요약·내 기록 찾기·childKeys) ③ 배포 후 휴대폰 확인 경로는 보고서 참조.
**남은 코드 작업**: A-2단계(집계 비정규화 후 규칙 축소), 로그인 직후 사이트 전체 "가져올 기록 N건" 알림(auth_ui.js — 29페이지 캐시 갱신 필요해 보류), 메뉴에 '대회 결과' 추가(auth_ui.js MENU — 같은 이유로 보류).
| 2026-09-07 | 대회 참가: 인솔자(클럽장) 본인도 선수로 참가(본인 담기·명단 패널 본인 행·저장 self/uid·종료 시 선수 이력) | 완료·배포 ef25077 |
| 2026-09-07 | 대회 공고 날짜별 시간대 입력(daySchedule)·상세 타임라인·작성 폼 접이식 + 배너 필터 완화 | 완료·배포 ef25077 |
| 2026-09-07 | 서브페이지 체육회 스타일(서브 내비·타이틀·입력칸·표) + 관련사이트 스포츠안전재단 + 관리자 [사이트 설정] 탭(siteContent/site) | 완료·배포 8a069c7 |
| 2026-09-07 | 이모지 전면 교체 icons.js(선 아이콘 90종, 이모지 172종 매핑, 표시 시점 치환) | 완료·배포 1e5c077 |
| 2026-09-09 | 공고 시인성(대회 상세 .ckv 표·목록 날짜 배지·심판 머리 표), 참가비 중복 제거, 일정 세부 전폭 배치 | 완료·배포 |
| 2026-09-09 | 입상 기록 등록 신청(awardClaims: 마이페이지 신청·관리자 승인→경력 기록) — 규칙 v20 게시 필요 | 완료·배포 |
| 2026-09-09 | 회원관리 유형 필터, 경력 엑셀 읽기 오류(app.xml) 복구, 국가자격 체크박스 통일(연맹 자격란 제거), 아이디 로그인·가입(학생 인증 생략·이메일 선택·알림 동의), 기존 계정 아이디 설정 — 규칙 v21 게시 필요 | 완료·배포 |
| 2026-09-09 | 홈→개별 공고 연결(캘린더 일정 slotId·competitions 대조·calendar ?id·license ?id·notice ?i), 대회·심판 공고 권역 지도 필터, 서브페이지 체육회화 2(--red→파랑, 검정 상자→네이비) | 완료·배포 |
| 2026-09-09 | 하위 메뉴 개별 화면(뷰 분리) 1~4단계: 대회 3화면·컴팩트 공고 행, 클럽 4·자격증 3·강사 3, 연맹소개 6·종목 5(경기 규칙 신설)·사업안내 4·가이드 6·FAQ 3·리더 4·알림마당 2, 메뉴 전면 개별 주소 | 완료·배포 |
